using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums {
    public enum Panels {
        DEFAULT,
        QR_READER,
        SEARCH,
        SETTINGS,
        RESULT
    }
}
